import { StyleSheet } from "react-native";

export const generalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
    paddingTop: 0,
    paddingBottom: 24,
    flexDirection: "column",
  },
  debug: {
    borderWidth: 1,
    borderColor: "red",
  },
});
